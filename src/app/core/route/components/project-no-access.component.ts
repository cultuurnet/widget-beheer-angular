import { Component, Input } from '@angular/core';

/**
 * Component with html when a user has no access to a project.
 */
@Component({
  templateUrl: './project-no-access.component.html',
})
export class ProjectNoAccessComponent {}
